APETIT_DB_PATH=apetit.db
PORT=8000

# --------------------------------------------------------------------------
# Entrada no app: e-mail e codigo de 6 digitos
# --------------------------------------------------------------------------

# Segredo do servidor usado para guardar o codigo como hash. Tem de ficar FORA
# do banco: e ele que impede que um banco vazado entregue as contas — seis
# digitos sao um milhao de possibilidades, e um hash sem segredo se quebra em
# segundos. Gere um novo e nao reaproveite de outro sistema:
#     python -c "import secrets; print(secrets.token_urlsafe(48))"
# Vazio = a entrada por codigo fica indisponivel (porta ausente, nao
# destrancada).
APETIT_SEGREDO=

# Por onde o codigo e enviado. Sem HOST e FROM, a rota de entrar responde
# indisponivel em vez de fingir que enviou.
APETIT_SMTP_HOST=
APETIT_SMTP_PORT=587
APETIT_SMTP_USER=
APETIT_SMTP_PASSWORD=
APETIT_SMTP_FROM=
APETIT_SMTP_TLS=1

# Quem pode entrar e definido por `scripts/autorizar.py`, e nao por variavel de
# ambiente: a lista muda quando alguem entra ou sai da empresa, e viver no banco
# e o que permite revogar sem reiniciar o servidor.

# --------------------------------------------------------------------------
# Publicar cardapio
# --------------------------------------------------------------------------

# Token para a operacao publicar por HTTP (POST /api/cardapio). Publicar e a
# acao de maior privilegio do sistema: quem publica define o que as pessoas leem
# sobre alergenico. Vazio = a rota recusa tudo.
APETIT_PUBLICAR_TOKEN=
