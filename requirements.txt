# Tornado atende a API (`apetit/api.py`). Vinha de carona no
# python-telegram-bot[webhooks]; com o Telegram fora, entra como dependencia
# propria — que e o que sempre foi.
tornado==6.5.8

# Planilha de cardapio e de receitas.
openpyxl==3.1.5

# Ficha do nutricionista em PDF.
pypdf==6.18.1

# `.env` nos scripts de operacao.
python-dotenv==1.2.2
